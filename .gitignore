.lore/memory.local.md
MEMORY.local.md
site/
node_modules/
docs/knowledge/local/
.lore/links
.env

# Windows: AI agents may run `> nul` (cmd syntax) in Git Bash, creating a literal file
nul

# Nav generated by Docker at runtime — not tracked in source
mkdocs.yml

# Skills and fieldnotes copied into docs/ by generate-nav.sh — build artifact, not source
docs/skills/
docs/fieldnotes/

# Prevent embedded repo from /lore-update temp clone landing in project root
lore/
