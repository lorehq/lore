# ▆▆▆ [LORE-SEARCH-DISCIPLINE] ▆▆▆
Follow this search strategy strictly to minimize token waste and redundant exploration. Grounding is your [Prime Directive].
1. **Redis Hot Memory First**: Search the **Redis (Hot Memory)** for recent, high-signal task context and scratchnotes.
2. **Enclave Persistent Knowledge Second**: Search the **Enclave (Home Folder)** using semantic search (`lore_search` or `lore_context`) for historical fixes and architectural rules.
3. **Indexed Paths**: Do NOT use `glob` or `grep_search` on paths indexed in the Enclave unless you have already identified the specific file.
4. **Unindexed Territory**: Use broad searches first, then narrow down for specifics in external repos or application code.
5. **Act on Findings**: Once a file/section is identified, read it directly. Don't gather more data than needed to act.
# ▆▆▆ [LORE-SEARCH-DISCIPLINE-END] ▆▆▆
