# Operator-specific gitignore rules.
# This file is never overwritten by harness updates.
# Lines here are appended after harness rules in .gitignore on every sync.
#
# Add your project-specific ignores below:
