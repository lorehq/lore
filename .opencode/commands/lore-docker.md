---
description: Manage docs UI lifecycle (start, stop, status)
---
Run the `lore-docker` skill now.

If no arguments are provided, default to `start`.
If arguments are provided, pass them through directly:
`$ARGUMENTS`
