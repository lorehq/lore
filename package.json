{
  "name": "lore",
  "version": "0.8.1",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "eslint .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1"
  }
}
