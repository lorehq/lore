# STOP — Global Setup Required

**Lore global environment is not configured.** You MUST run `/lore-setup` before doing anything else.

Do NOT proceed with any task until setup is complete. Do NOT skip this step. Inform the operator that global setup is required and offer to run `/lore-setup` now.

Setup will configure:
- Operator profile (role, coding style, communication preferences)
- Machine context (OS, installed runtimes, key tools)
- Environment details (services, platforms, infrastructure)

This stub is replaced with real instructions after setup. Until then, this message will appear in every project's mandate file.
